import { PkceLaunchButton } from "@/components/PkceLaunchButton";

# OAuth2 with SMART on FHIR
We'll run through an example flow that uses the PKCE OAuth2 flow
to get an access token for a protected FHIR server.

## Launch
First, we need to redirect the user to their sign-in page.
<PkceLaunchButton />