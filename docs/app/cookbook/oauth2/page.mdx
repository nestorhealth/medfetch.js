import { PkceLaunchButton } from "@/components/PkceLaunchButton";

# OAuth2
We'll run through an example flow that uses the PKCE OAuth2 flow to get an access token for a protected FHIR server.
This flow is outlined by the [SMART on FHIR]() specs for a public/frontend-only client.

## Launch
First, we need to redirect the user to their sign-in page.
<PkceLaunchButton />