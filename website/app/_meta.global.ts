import { MetaRecord } from "nextra";

const globalMeta: MetaRecord = {
  index: {
    type: "page",
    display: "hidden",
  },

  showcase: {
    type: "page",
    title: "Showcase",
  },
};

export default globalMeta;
